"use strict";

exports.__esModule = true;
exports.useChakra = useChakra;
exports.useToken = useToken;

var _colorMode = require("@chakra-ui/color-mode");

var _utils = require("@chakra-ui/utils");

var _providers = require("./providers");

function useChakra() {
  var _useColorMode = (0, _colorMode.useColorMode)(),
      colorMode = _useColorMode.colorMode,
      toggleColorMode = _useColorMode.toggleColorMode;

  var theme = (0, _providers.useTheme)();
  return {
    colorMode: colorMode,
    toggleColorMode: toggleColorMode,
    theme: theme
  };
}

function useToken(scale, token, fallback) {
  var theme = (0, _providers.useTheme)();
  var path = scale + "." + token;
  return (0, _utils.get)(theme, path, fallback != null ? fallback : token);
}
