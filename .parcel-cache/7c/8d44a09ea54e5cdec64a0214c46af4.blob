"use strict";

exports.__esModule = true;
exports.createSlideBaseStyle = exports.createSlideConfig = void 0;
var placements = {
  bottom: {
    maxWidth: "100vw",
    bottom: 0,
    left: 0,
    right: 0
  },
  top: {
    maxWidth: "100vw",
    top: 0,
    left: 0,
    right: 0
  },
  left: {
    width: "100%",
    height: "100vh",
    left: 0,
    top: 0
  },
  right: {
    width: "100%",
    right: 0,
    top: 0,
    height: "100vh"
  }
};
var offset = {
  bottom: "100%",
  top: "-100%",
  left: "-100%",
  right: "100%"
};

var trans = function trans(placement, value) {
  if (["top", "bottom"].includes(placement)) {
    return "translate3d(0, " + value + ", 0)";
  }

  return "translate3d(" + value + ", 0, 0)";
};

var createSlideConfig = function createSlideConfig(placement) {
  return {
    enter: {
      from: {
        transform: trans(placement, offset[placement])
      },
      to: {
        transform: trans(placement, "0%")
      }
    },
    exit: {
      from: {
        transform: trans(placement, "0%")
      },
      to: {
        transform: trans(placement, offset[placement])
      }
    }
  };
};

exports.createSlideConfig = createSlideConfig;

var createSlideBaseStyle = function createSlideBaseStyle(placement) {
  return placements[placement];
};

exports.createSlideBaseStyle = createSlideBaseStyle;
